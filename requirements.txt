# Pi deployment requirements (used by services/install.sh).
# Canonical dependency metadata lives in pyproject.toml; this file adds
# pyfftw, which is worthwhile on the Pi (NEON SIMD) but optional elsewhere.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiosqlite>=0.20.0
apscheduler>=3.10.4
pyyaml>=6.0.1
websockets>=12.0
python-multipart>=0.0.9
pyrtlsdr>=0.2.7,<0.3.0
av>=12.0.0
numpy>=1.24.0
scipy>=1.11.0
pyfftw>=0.13.0
